import React from 'react'
import { RateProps } from '../../types/rate'
import '../../style/components/rate/index.scss'
import { View } from '@tarojs/components'

function Rate(props: RateProps) {
  return <View className='fta-rate'></View>
}

export default Rate
